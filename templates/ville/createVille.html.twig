{% extends 'base.html.twig' %}

{% block title %}Création d'une ville{% endblock %}

{% block body %}
<main class="form-signin text-center">
    <div class="row text-center">
        <h2>Ajouter une ville</h2>
    </div>

        {{ form_start(createVilleForm) }}

            {{ form_label(createVilleForm.nom, "Nom", {'label_attr' : {'class' : 'form-label'}}) }}
            {{ form_errors(createVilleForm.nom) }}
            {{ form_widget(createVilleForm.nom, {'attr' : {'class' : 'form-control' , 'placeholder': 'Nom'}}) }}

            {{ form_label(createVilleForm.codePostal, "Code Postal", {'label_attr' : {'class' : 'form-label'}}) }}
            {{ form_errors(createVilleForm.codePostal) }}
            {{ form_widget(createVilleForm.codePostal, {'attr' : {'class' : 'form-control' , 'placeholder': 'Code Postal'}}) }}



            <button class="btn btn-lg btn-secondary " type="submit">Ajouter une ville</button>

        {{ form_end(createVilleForm) }}
    </main>
{% endblock %}

